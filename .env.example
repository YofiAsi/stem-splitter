LOG_LEVEL=info
# Host port for the UI + API (same origin)
PORT=4000
# Where job files and the SQLite DB live (inside the container / on the volume)
STEMS_DIR=/var/stems
SQLITE_PATH=/var/stems/db.sqlite
# Separation device: cuda (GPU) or cpu
DEMUCS_DEVICE=cuda
